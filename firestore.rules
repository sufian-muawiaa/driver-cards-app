rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // قنوات المزامنة بين الأجهزة
    match /channels/{channel}/{document=**} {
      allow read, write: if true;
    }
  }
}
